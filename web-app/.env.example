VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=

VITE_APPLE_CLIENT_ID=
VITE_APPLE_TEAM_ID=
VITE_APPLE_KEY_ID=
# For Apple, you may store the private key text or a path; keep it secure
VITE_APPLE_PRIVATE_KEY=

VITE_FACEBOOK_APP_ID=
VITE_FACEBOOK_APP_SECRET=

# OAuth redirect URI used by your providers (example)
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback

# URL of the auth server that performs server-side token exchanges
# Default used by code: http://localhost:3000
VITE_AUTH_SERVER_BASE=http://localhost:3000

# Notes:
# - In Vite, env variables that should be exposed to the client must be prefixed with VITE_.
# - Keep real secrets out of client builds; prefer sending server-side tokens from a backend.

# Optional: base URL for resource-server so the front-end can call it directly
# e.g. VITE_RESOURCE_SERVER_BASE=http://localhost:4000
VITE_RESOURCE_SERVER_BASE=http://localhost:4000

# Vite dev server port (override when running `npm run dev -- --port <port>`)
VITE_DEV_PORT=5173
