# RESOURCE_STORE: 'memory' (default), 'dynamo' or 'postgres'
# For local tests you may set RESOURCE_STORE=memory
# To use Postgres set RESOURCE_STORE=postgres and configure `DATABASE_URL`.
# By default this project now targets Postgres RDS for the resource store.
RESOURCE_STORE=postgres
AWS_SDK_LOAD_CONFIG=1
# DynamoDB settings (only used when RESOURCE_STORE=dynamo)
AWS_REGION=us-east-2
DYNAMO_TABLE=Resources

# Server port (override to run on a different port or to run multiple instances)
PORT=4000

# Optional: point at LocalStack or a custom DynamoDB endpoint
# DYNAMO_ENDPOINT=http://localhost:4566

# Postgres settings (only used when RESOURCE_STORE=postgres)
# Example local URL:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/resources
#
# When using the CDK-provisioned RDS instance, retrieve credentials from
# Secrets Manager (CDK output `ResourcesDbSecretArn`) and set `DATABASE_URL`.
# Example (using AWS CLI):
# aws secretsmanager get-secret-value --secret-id <secret-arn> --profile dev --region us-east-2
# The returned SecretString will contain fields like username, password, host, port, dbname.
# Then construct:
# DATABASE_URL=postgres://<username>:<password>@<host>:<port>/<dbname>

# Optional: use a named AWS profile (the SDK will pick up shared credentials)
AWS_PROFILE=dev
