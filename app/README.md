# Hello React App

Minimal React app scaffolded with Vite.

Run locally:

```powershell
cd hello-react-app
npm install
npm run dev
```

Open the URL printed by `vite` (usually http://localhost:5173).
